# Dependencies
node_modules/

# Local database file (don't commit this)
database.db

# npm cache
.npm-cache/
npm-cache/

# DSH internal files
.dsh-file-claim.audit.jsonl
.dsh-file-claim.lock
*.dshclaim

# Environment variables (keep secrets out of git)
.env

# A3 email outbox (local inspection/test artifact of the "file" transport)
.mailoutbox.json
.mailoutbox-*.json
